import * as React from 'react'

// Minimal placeholder; avoids runtime errors even if no global timer store exists.
// If you want the floating panel back later, we can wire it to a shared store.
export default function FloatingTimers() {
  return null
}
